{
  "name": "league-api",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve -d dist src/index.html",
    "build": "parcel build --dist-dir build --public-url /lol/ src/index.html",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "parcel": "^2.13.2"
  },
  "homepage": "./",
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
